import 'package:flutter/material.dart';

class Menu5Page extends StatelessWidget {
  const Menu5Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Este é o menu que conterá o perfil do usuário.'),
    );
  }
}
